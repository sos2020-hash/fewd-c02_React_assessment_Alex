const userAccessToken 

const getAccessToken = (userAccessToken) => {
        if(userAccessToken) {
            return userAccessToken
        } else {
            
        }
}
const Spotify = {
 
}

export {Spotify}